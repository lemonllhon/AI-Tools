# 更新日志

简体中文 · [English](CHANGELOG.md)

本文件记录 Cockpit Tools 的所有重要变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [0.0.52] - 2026-06-09

### 变更
- 优化 Codex 大量账号场景下的删除流程，批量处理索引更新、账号文件删除、OAuth 绑定清理和本地 API 服务引用清理。
- Codex 账号总览删除成功后会立即从前端状态中移除账号，不再阻塞等待全量账号重新加载。

### 修复
- 删除 Codex 账号后会同步移除本地 API 服务成员、路由规则、绑定 OAuth、账号预处理缓存和路由亲和状态，避免已删除账号拖慢后续请求。
- 进行中的 Codex 账号列表刷新不会再把刚删除的账号重新插回总览。

## [0.0.51] - 2026-06-09

### 变更
- 优化 Codex 账号总览在大量账号下的加载性能，合并重复账号请求、限制重型资料补全，并避免写入过大的本地缓存。
- 优化 Codex 额度刷新，后台自动刷新只检查过期或可重试账号，手动“刷新全部”仍保持强制刷新。
- Codex 本地 API 服务状态刷新现在会避免重复全量扫描，并按有效服务成员统计账号和供应商 Key。

### 修复
- 新增的 Codex OAuth、API Key 和 JSON 导入账号现在会继承已保存的全账号速度；开启自动加入时也会进入本地 API 服务账号池。
- Codex 批量导入现在会一次性同步本地 API 服务自动加入，不再按账号反复重写服务集合。
- 临时额度刷新失败不再触发账号自动物理删除。

## [0.0.50] - 2026-06-09

### 变更
- Codex 账号总览现在会自动物理删除 error 账号，并同步从本地 API 服务账号池中移除。
- Codex 本地 API 服务路由现在会跳过已删除或异常账号，并继续使用剩余有效账号处理请求。

## [0.0.49] - 2026-06-09

### 变更
- Codex 2倍速度状态改为蓝色，不再使用黄色。

## [0.0.48] - 2026-06-09

### 变更
- Codex 速度入口现在只保留“标准”和“2倍”；2倍配置会归并到同一个 2倍档位。

## [0.0.47] - 2026-06-08

### 修复
- Codex API 服务现在会在转发 Responses 请求时按选中账号的速度写入对应 Codex 服务档位，让“全账号速度”真正作用到上游请求。
- 自动加入的 New API 供应商 Key 现在会继承已保存的 API 服务速度，供应商池和融合模式不再回落到标准倍率。
- 修改单账号或全账号速度后会清理本地接入的账号预处理缓存，避免短时间继续按旧倍率路由。

## [0.0.46] - 2026-06-08

### 修复
- Codex API 服务自动加入的账号现在会继承已保存的 API 服务速度设置，不再保持默认标准倍率。
- 在服务面板开启“自动加入”时，本次保存中新成为服务成员的账号也会应用已保存的 API 服务速度，同时不会覆盖已有成员速度。

## [0.0.45] - 2026-06-07

### 变更
- 将全局自动刷新从固定轮询改为按任务到期时间唤醒，降低空闲 CPU 和 WebView2 GPU 占用。
- 实例列表后台刷新频率降至 30 秒，并在应用窗口隐藏时暂停刷新。
- 2FA 倒计时在窗口隐藏时暂停，回到前台后立即校准刷新。

### 修复
- 移除侧栏更新进度波浪和 Codex API 服务活跃账号呼吸高亮的常驻动画，避免 WebView 合成器持续忙碌。
- 侧栏从高成本模糊背景改为实体背景，降低持续 GPU 合成开销。

## [0.0.44] - 2026-06-07

### 修复
- Windows portable 免安装包现在会在上传前强制包含并校验内置代理内核资源，修复打开代理内核目录时提示缺少 `runtime-manifest.json` 的问题。

## [0.0.43] - 2026-06-07

### 变更
- Codex API 服务切到局域网访问时，服务面板现在会显示检测到的局域网接入地址，方便同一局域网内的其他设备接入。
- 仪表盘 Codex API 服务卡片在启用局域网访问时，会优先展示局域网接入地址，让统一入口也能直接看到共享端点。

### 修复
- 服务面板在未检测到本机局域网 IP 时不再引导复制 `0.0.0.0`，改为显示提示信息。

## [0.0.42] - 2026-06-07

### 新增
- Codex 速度新增真正的 2 倍档位，账号速度下拉、仪表盘“全账号速度”和 Codex API 服务面板均已支持。

### 变更
- Codex API 服务“全账号速度”改为后端批量更新，不再由前端逐账号循环调用，大账号池设置速度时更稳定。
- Codex 自动切号账号范围选择器新增分页，避免上千账号一次性渲染造成卡顿。
- Codex 账号按主额度排序时会回退使用唯一存在的额度窗口，与 Free 账号单额度行为保持一致。

### 修复
- Codex 速度持久化现在会保留官方 `flex` 服务档位，不再误读成 `fast`，避免 2 倍速状态看起来被重置。
- 仪表盘和 Codex API 服务面板的速度汇总现在会同时统计标准、快速和 2 倍，正确高亮当前全账号速度。

## [0.0.41] - 2026-06-07

### 修复
- 仪表盘和悬浮卡推荐 Codex 账号时，现在只按实际存在的额度窗口计算平均值，Free 单额度账号不会再被不存在的窗口拉低排序。

### 变更
- 同步设置页 Codex 自动切号文案，使其与快速设置和后端的主额度/单额度行为保持一致。

## [0.0.40] - 2026-06-07

### 修复
- Codex 自动切号现在会在账号没有 5 小时主窗口、只返回一个额度窗口时使用该唯一额度判断阈值，Free 账号额度低于指定比例后也能触发切号。
- Codex 本地接入按自动切号阈值跳过低额度账号时，也会使用同一套主额度/单额度判断。

### 变更
- 优化 Codex 自动切号阈值设置文案，明确主额度和 Free 单额度的适配行为。

## [0.0.39] - 2026-06-07

### 变更
- 优化 Codex 大批量账号导入性能，JSON 数组、JSON Lines、Token 多行、Sub2API 和多文件导入会复用账号索引状态，减少重复索引写入。
- Codex 账号列表读取新增短时后端缓存，大量账号反复刷新时会减少详情文件扫描。
- Codex 账号资料补全改为限量批处理，并优先处理当前可见分页，避免打开上千账号列表时卡顿。
- 大批量 Codex 导入会跳过导入后的逐账号配额刷新，并节流文件导入进度 UI 更新，降低导入后的界面停顿。

## [0.0.38] - 2026-06-06

### 新增
- Codex 账号总览新增批量导入账号 JSON 文件入口，导入进度和结果会同步显示在总览消息条中。
- Codex 本地文件导入现在明确支持一次选择多个文件，导入 Codex 账号 JSON、`auth.json`、session JSON、tokens 数组和 Sub2API JSON。

### 修复
- Codex API Key 账号 JSON 导入现在兼容 `openai_api_key` 和 `openaiApiKey` 字段别名，不再只识别 `OPENAI_API_KEY`。

## [0.0.37] - 2026-06-03

### 新增
- Codex API 服务“全账号速度”现在会纳入已选 New API 供应商 Key，只有供应商池或融合模式时也可从仪表盘和服务面板统一设置 API 服务默认速度。

### 修复
- 手动取消 API 服务成员时会自动关闭账号“自动加入”，避免刷新后该成员被立刻加回。
- 手动取消 New API 供应商时会自动关闭供应商“自动加入”，保存后的供应商号池选择会被正确保留。

## [0.0.36] - 2026-06-02

### 新增
- 仪表盘 API 服务控制台、Codex 总览 API 服务卡片和 API 服务面板新增“更新配置”，可重新写入当前 Codex `config.toml`/认证供应商配置，不重启 API 服务。
- 新增 API 服务活跃来源呼吸提示：账号池或融合模式运行时高亮正在使用的账号池成员，供应商或融合模式运行时高亮对应 New API 模型供应商卡片。

### 变更
- New API 模型供应商卡片不再显示“引用账号 0 个”，仅在确实有关联账号时展示引用数量。

### 注意
- “更新配置”会立即刷新写入的本地接入端点、API Key 和 WebSocket 支持信息；已经运行中的 Codex 客户端是否免重启生效，仍取决于客户端是否重新读取配置。

## [0.0.35] - 2026-06-02

### 新增
- Codex API 服务面板新增模型供应商统计，可查看请求总量、成功/失败次数、Token 用量、平均延迟、端点和 Key 数。

### 修复
- API 服务激活时会把解析后的 WebSocket 支持状态写入 `config.toml`，关闭 WS 协议后当前 Codex 本地接入供应商配置会同步生效。
- 会在影响当前本地接入供应商的 API 服务设置变更后刷新已写入的端点、Bearer Token 和 WebSocket 支持信息。

## [0.0.34] - 2026-06-02

### 新增
- Codex API 服务新增 WS 协议模式切换，可选择“自动”“启用”或“禁用”。

### 变更
- 自动模式会在供应商来源下关闭 Codex Responses WebSocket 偏好，让旧版 New API 上游可回退到普通 HTTP/SSE 调用。

### 修复
- 当 API 服务 WS 协议模式解析为禁用时，会拒绝 WebSocket upgrade 请求，避免客户端继续使用不受支持的 WS 路径。
- 修复 API 服务只有供应商号池、没有 OAuth 账号池成员时，Codex 客户端模型列表可能不可用的问题。

## [0.0.33] - 2026-06-02

### 新增
- Codex API 服务新增“管理供应商”，可将已保存的 New API 模型供应商加入供应商号池，并支持自动加入新的 New API 供应商。

### 变更
- Codex API 服务独立供应商来源模式文案统一为“供应商”，减少与融合模式的歧义。
- 供应商模式和融合模式现在可直接使用模型供应商里的 New API Key，不再要求先把供应商 Key 添加成 Codex 账号。

### 修复
- 修复 New API 供应商流式响应只返回 `response.done` 或提前结束时，Codex 客户端可能报 `stream disconnected before completion` 的问题。

## [0.0.32] - 2026-06-02

### 新增
- Codex API 服务新增来源模式切换，可选择“供应商”“账号池”或“融合模式”。

### 变更
- 调整 Codex API 服务路由逻辑：融合模式会优先使用 New API 供应商账号，供应商不可用时自动回退到 OAuth 账号池；独立模式可强制只使用供应商或只使用账号池。

## [0.0.31] - 2026-06-01

### 变更
- New API 模型供应商预设改为只展示外网域名占位端点，避免在供应商选择器里默认出现 localhost 地址。

## [0.0.30] - 2026-06-01

### 新增
- Codex 本地 API 服务支持 New API API Key 账号加入账号池，请求会携带该账号 API Key 转发到配置的 New API `/v1` Base URL。
- New API 供应商 ID 兼容 `newapi` 写法，与 `new_api` 一样用于供应商元数据、额度展示、徽标和本地 API 服务准入判断。

### 变更
- Codex 本地 API 服务自动路由策略会优先尝试 New API 账号，再尝试 OAuth 账号，从而让 New API 自身的渠道池优先接管调用。
- API 服务账号选择与健康检测现在允许 OAuth 账号和 New API API Key 账号，其他 API Key 供应商仍不会加入本地账号池。

## [0.0.29] - 2026-06-01

### 新增
- Codex API Key 账号新增 New API 模型供应商预设，内置本地 New API 默认地址和供应商显示名称。
- 新增 Codex New API 额度读取，使用 New API 的 Token Usage 接口获取 Key 剩余额度。
- Codex 本地 API 服务模型列表和显示名映射新增 `gpt-5.5`。

### 变更
- 调整 Codex API Key 账号元数据，New API 账号会保留专属供应商、套餐标识、默认账号名和导入账号身份。
- 调整 Codex 供应商配置生成，New API 会写入专属自定义模型供应商，API Key 运行时转发仍使用固定本地接入供应商。

## [0.0.28] - 2026-05-27

### 新增
- Codex 设置新增自动切号控制，可开启无感切换、设置账号范围，并默认使用 10% 的 5 小时配额阈值。

### 变更
- Codex 自动切号改为以 5 小时配额作为触发和候选排序依据，避免周配额阻止无感切号。
- Codex 本地 API 服务会无感跳过 5 小时配额低于或等于自动切号阈值的账号，并继续路由到额度更健康的账号。

## [0.0.27] - 2026-05-27

### 修复
- 优化 Codex OAuth Token 交换失败提示，补充状态码、响应长度和授权服务错误码信息。
- Codex OAuth 收到 403 后会切换到下一个内置节点池出口并重试一次，降低当前出口被授权服务拒绝时的登录失败概率。

## [0.0.26] - 2026-05-27

### 新增
- 仪表盘 API 服务控制台新增 Codex 全账号速度一键设置，可统一切换所有 Codex 账号为“标准”或“快速”。
- Codex API 服务面板新增同样的全账号速度入口，应用时会同步更新 API 服务默认速度。

## [0.0.25] - 2026-05-24

### 新增
- 多国语言功能。

## [0.0.24] - 2026-05-23

### 新增
- 新增内置代理网关的受控重启处理，应用更新和重启前会先关闭代理网关监听、桥接内核进程以及 Codex API 本地接入服务。
- 新增启动时残留桥接清理，会扫描旧的代理桥接工作目录、PID 文件和监听端口，清掉上次会话遗留的桥接进程。
- 新增节点列表头部的订阅来源多选，可以按一个或多个订阅来源联动筛选节点列表。

### 变更
- 调整 Windows 代理桥接清理方式，使用 `taskkill /F /T` 结束完整进程树，减少 mihomo/sing-box/xray 子进程和端口残留。
- 调整节点列表来源筛选行为，当搜索名称/地址/分组、切换分组或协议筛选时，来源范围会自动恢复为全部来源，让结果更直观。
- 调整节点列表头部控件高度，订阅来源多选、已选择按钮和折叠节点列表按钮保持同一紧凑高度。

### 修复
- 修复应用更新、重启或网关重启后，内置代理网关和桥接端口偶发占用不释放的问题。
- 修复“添加资源”预览大订阅后，延迟测速和 IP 健康检测可能一直显示“测速中/检测中”的问题；现在预览桥接节点会有限并发执行，并对单节点加入超时兜底。
- 修复旧的“显示范围”订阅来源切换与新的节点列表来源筛选功能重复的问题。
- 修复节点来源筛选只能选择单个订阅，无法同时查看多个订阅来源节点的问题。

## [0.0.23] - 2026-05-23

### 修复
- 修复 `proxy_pool::gateway` 在 Release 构建中因返回网关故障切换成功出口引用但缺少显式生命周期而编译失败的问题。
- 修复 HTTPS 上游代理 TLS 配置对 `rustls-native-certs` 0.8 新证书结果结构不兼容的问题；系统根证书部分加载失败现在记录为警告，不再阻断可用证书继续使用。

## [0.0.22] - 2026-05-23

### 新增
- 新增内置代理网关的 HTTPS 上游代理支持，包括到上游代理的 TLS 连接、系统根证书校验、CONNECT 隧道、普通 HTTP absolute-form 转发，以及复用上游代理认证。
- 新增批量延迟测速和批量 IP 健康检查的后端实时进度事件，按任务 ID 发送 `started`、逐节点 `node_done` 和 `finished` 阶段。
- 新增导入预览阶段的临时延迟测速和 IP 健康检查，粘贴资源与 URL 订阅预览后都可以先对所选预览节点检测再导入。
- 新增代理网关故障切换事件，当节点池备用节点接管为当前出口时，“网络服务”界面会同步更新当前出口显示。

### 变更
- 调整代理内核运行时解析逻辑，优先识别软件安装目录下的 `proxy-runtime` 打包资源，再回退到用户缓存目录，同时保持 Windows、macOS、Linux 多平台路径兼容。
- 调整“内置代理内核”设置面板，支持折叠显示，并可直接打开打包内核资源目录。
- 调整批量延迟测速和 IP 健康检查流程，每个节点完成后立即持久化结果并更新前端进度，不再等整批结束才刷新。
- 调整节点池网关路由，当前节点失败后如果备用节点转发成功，会在安全条件内把该备用节点持久化为 `current_node_id`，并加入冷却保护避免并发请求反复覆盖。
- 更新代理网络服务实现方案文档，记录 HTTPS 上游代理、实时进度、预览检测和故障切换持久化的阶段完成状态。

### 修复
- 修复 HTTPS 代理节点无法作为内置网关上游使用的问题，之前网关只支持明文 HTTP 代理转发。
- 修复批量代理诊断耗时较长时界面像卡住一样没有逐节点反馈的问题。
- 修复导入预览节点必须先真正导入后才能临时测速或检查 IP 健康的问题。
- 修复节点池故障切换只对当前请求生效，后续请求仍优先尝试失败节点的问题。
- 修复设置页打开时，自动网关故障切换后的当前出口显示不会实时更新的问题。

## [0.0.21] - 2026-05-23

### 变更
- 调整 Codex API 本地接入端口清理流程，清理后会等待网关端口释放，并在重新确认监听状态后把“占用 PID 已退出”视为非致命情况。
- 调整端口清理结果和前端提示，当 API 服务从旧端口自动切换到新的可用端口时会明确显示旧端口与新端口。

### 修复
- 修复 Windows 端口清理中 `netstat` 找到 PID，但 `taskkill` 返回“PID 不存在”时会直接失败的问题；现在会重新确认进程和端口是否仍然存活。
- 修复 macOS 端口清理中 `lsof` 找到的监听 PID 在清理过程中已退出时仍被当作硬失败的问题，同时保留等待端口释放和必要时换端口的统一逻辑。
- 修复 Linux 端口占用探测只依赖 `lsof` 的问题，现在额外使用 `ss -ltnp` 和 `netstat -ltnp` 兜底，让精简环境也能清理或恢复 API 服务端口。

## [0.0.20] - 2026-05-23

### 变更
- 将代理资源导入里的“预览导入”按钮改为“预览”，保留“导入所选”作为真正写入节点的操作。
- 调整代理资源导入预览逻辑，预览后默认不再自动勾选全部节点，需要用户明确选择后再导入。
- 调整添加资源、URL 订阅、订阅编辑和手动添加节点表单，标签与输入内容统一左对齐。

### 修复
- 修复代理节点列表行内出现重复选择框的问题，移除右侧单独的批量删除勾选框，改为复用前置节点选择框执行所选节点批量操作。
- 修复导入预览阶段节点被默认选中，导致用户还没选择就容易直接导入全部节点的问题。
- 修复 IP 健康详情弹框按页面位置居中、点击眼睛后还需要滚动寻找弹框的问题，现在弹框按当前可视区域居中。
- 修复添加资源粘贴内容、分组、名称前缀、URL 订阅和添加节点表单字段视觉上没有统一左对齐的问题。

## [0.0.19] - 2026-05-23

### 变更
- 调整代理延迟测速逻辑，按顺序尝试多个 `generate_204` 探测地址，减少单个探测站点被阻断或临时网关错误导致的误判。
- 调整 mihomo 桥接启动方式，为每个节点使用独立工作目录，并关闭 profile 缓存持久化，降低重复检测或批量检测时的 cache 文件竞争。
- 调整 WebSocket 桥接字段映射，分享链接里的 `host` 只作为 WS `Host` 请求头，不再误复用为 TLS `servername`。

### 修复
- 修复批量延迟测速过度依赖 `cp.cloudflare.com/generate_204`，导致节点实际可用但测速失败的问题。
- 修复 VLESS/WS/TLS 节点因为 WS Host 与 TLS servername 没有清晰分离，更容易触发 mihomo `unexpected status: 502 Bad Gateway` 的问题。
- 修复内置代理网关重启时，前一个监听任务尚未完全释放就误报网关端口被占用的问题。
- 修复 Codex API 本地接入修改端口时可能先启动旧端口，导致提示 `127.0.0.1:<port> 已被占用` 的问题。

## [0.0.18] - 2026-05-23

### 变更
- 调整临时 mihomo 桥接就绪逻辑，延迟测速和 IP 健康检查在本地 mixed/SOCKS 端口可连接后会短暂等待，让 mihomo compatible provider 完成初始化。
- 调整延迟测速探测地址，从 Google `generate_204` 切换为 Cloudflare `generate_204`，减少区域网络可达性干扰。
- 优化延迟测速和 IP 健康失败信息，错误中会附带 reqwest 底层 cause 链和桥接日志片段，便于定位连接中断、重置或 tunnel 失败。

### 修复
- 修复选择订阅节点后，内置网关可以正常代理，但立即执行延迟测速或 IP 健康检查时，可能因为 mihomo 已监听端口但 provider 尚未准备完成而失败的问题。
- 修复高级节点检测失败时底层请求原因不够清晰的问题。

## [0.0.17] - 2026-05-23

### 新增
- 新增代理桥接诊断信息，网关转发、延迟测速和 IP 健康检查失败时会带出 mihomo 内核日志片段。
- 新增桥接启动与就绪日志，记录内核类型、节点、本地 SOCKS 端点、内核路径和生成的配置路径，便于排查发布构建环境问题。
- 新增 mihomo 桥接配置生成回归测试，确保选中的高级协议节点会通过预期代理组转发。

### 变更
- 调整 mihomo 桥接配置生成方式，改为标准 `proxies` + `proxy-groups` 结构，并通过 `MATCH` 明确路由到生成的代理组，不再依赖单个裸代理名称。
- 调整高级节点 TLS 字段兼容，读取更多订阅常见 SNI 字段，包括 `sni`、`servername`、`serverName`、`peer`、query `host` 和 Clash YAML options。
- 调整高级节点证书跳过校验兼容，支持识别分享链接中的 `allowInsecure`、`insecure`、`skip-cert-verify` 等参数。

### 修复
- 修复选择 VLESS/WS/TLS 订阅节点后，内置代理网关可能因 mihomo 路由配置缺少明确代理组而无法真正转发的问题。
- 修复延迟测速和 IP 健康检查失败时只显示泛化请求错误、看不到 mihomo 真实失败原因的问题。
- 修复网关失败时只显示笼统 CONNECT/tunnel 错误的问题，现在会暴露 WebSocket `502 Bad Gateway` 等可操作的节点错误。

## [0.0.16] - 2026-05-23

### 新增
- 新增 mihomo 内核打包支持，覆盖 Windows、macOS Intel、macOS Apple Silicon、Linux x86_64、Linux arm64。
- 新增以 mihomo 为主路径的高级协议桥接配置生成，支持 `vmess`、`vless`、`trojan`、`ss`、`hysteria`、`hysteria2`、`tuic`、`anytls` 等节点。
- 新增节点列表“当前筛选结果全选/取消全选”能力，可按当前订阅、搜索、分组、协议和已选择视图快速批量选择节点。

### 变更
- 调整内置代理网关桥接路径，选中节点池出口时优先使用 mihomo，xray 与 sing-box 先保留为后续兼容兜底。
- 调整延迟测速、IP 健康检查和真实网关转发流程，高级订阅节点会通过 mihomo 桥接，不再停留在协议不支持错误。
- 调整代理节点池标题区域，“已选择”按钮移动到节点数量后、折叠/展开节点列表按钮前。
- 调整代理节点池布局对齐方式，订阅信息、节点列表标题、节点状态和紧凑节点行统一左对齐。

### 修复
- 修复选择高级协议节点后，Codex API 流量无法通过内置网关真正走订阅节点的问题。
- 修复“网络服务”中代理节点池部分控件和内容看起来偏右对齐的问题。
- 修复大量筛选节点只能逐个手动选择的问题，现在可一键选择当前可见筛选结果。

## [0.0.15] - 2026-05-22

### 新增
- 新增延迟测速与 IP 健康检查的临时 xray/sing-box 桥接能力，让 `vmess`、`vless`、`trojan`、`ss`、`hysteria`、`hysteria2`、`tuic`、`anytls` 等高级节点可以通过内置内核完成检测。
- 新增完整 IP 健康结果持久化和前端类型支持，节点行可以打开详情，不再只依赖简短摘要。
- 新增 IP 健康“眼睛”详情弹窗，展示 IP、地区、风险分、住宅/广播标记、ASN 组织、来源、更新时间、错误信息和原始响应数据。
- 新增节点列表显示范围控制，当存在多个订阅来源时，可以在“显示全部”和单个订阅之间切换。
- 新增“已选择”节点视图，可跨多个订阅展示当前已选择作为节点池出口的节点。

### 变更
- 调整高级协议节点的延迟测速和 IP 健康检查流程，检测时自动创建临时桥接，请求结束后自动清理。
- 调整批量检测流程，普通节点继续并发检测，需要桥接的高级节点通过临时桥接会话安全处理。
- 调整节点列表筛选联动：搜索名称/地址/分组、切换分组或协议时会恢复到“显示全部”；切换订阅时会清空搜索、分组和协议筛选。
- 调整“代理节点池”面板，移除重复的网关地址、网关端口、外部本地代理端口控制项，这些设置仅保留在下方主“内置代理网关”区域。
- 调整节点列表高度计算方式，根据前 10 个实际渲染节点行动态计算，不再依赖固定写死高度。
- 调整节点列表滚动体验，列表滚动到顶部或底部后，鼠标继续滚动会接续滚动外层设置页面。

### 修复
- 修复高级节点检测仍提示“vless 节点需要先完成内置桥接后才能检测”的问题，现在会走真实临时桥接检测路径。
- 修复高级节点 IP 健康列表中残留旧桥接等待摘要的问题，读取节点列表时会自动归一化处理。
- 修复 xray 桥接配置生成中可能把 Clash YAML 的 `options.type` 误当作 xray 传输网络的问题。
- 修复大量订阅节点场景下，必须把鼠标移出节点列表才能继续滚动整个设置页的问题。

## [0.0.14] - 2026-05-22

### 新增
- 新增真正可用的内置代理网关服务，监听 `127.0.0.1` 并支持 HTTP 与 CONNECT 转发。
- 新增内置代理网关出口路由，支持“直连 / 本地代理 / 节点池”三种模式。
- 新增 HTTP 上游代理转发能力，可让内置网关复用外部代理软件，例如 `127.0.0.1:7890`。
- 新增 SOCKS5 上游代理转发能力，并支持可选用户名/密码认证。
- 新增应用启动后自动恢复内置代理网关状态，保存过的网关配置会自动生效。
- 新增 xray 桥接启动能力，支持 `vmess`、`vless`、`trojan`、`ss` 等高级节点协议。
- 新增 sing-box 桥接启动能力，支持 `hysteria`、`hysteria2`、`tuic`、`anytls` 等高级节点协议。
- 新增按节点生成桥接配置，配置落在代理池数据目录，并通过本地 SOCKS 桥接端口接回内置网关。
- 新增缓存内核二进制解析能力，让桥接启动可使用已准备好的 Windows、macOS、Linux 平台 xray/sing-box 内核。
- 新增代理池出口状态持久化，记录 `outlet_mode`、`current_node_id` 和 `selected_node_ids_json`。
- 新增节点池多选出口能力，用户可以选择多个代理节点，并保留一个当前活动节点。
- 新增单个节点延迟测试，以及对已启用/已选择节点的批量延迟测试。
- 新增单个节点 IP 健康检查，以及对已启用/已选择节点的批量 IP 健康检查，并持久化健康摘要。
- 新增前端服务层和类型定义，支持网关状态、出口模式、已选节点、延迟结果和 IP 健康结果。
- 新增“网络服务”中的延迟测试、IP 健康检查、节点状态刷新和节点池出口管理交互。

### 变更
- 调整“网络服务”的全局代理语义：启用后表示启用内置代理网关，并使用配置的网关端口。
- 调整 Codex API“跟随全局代理”逻辑：全局代理启用时使用同一个内置网关地址。
- 调整代理出口选择为“直连 / 本地代理 / 节点池”三选一互斥模型。
- 调整节点导入逻辑，导入后的节点不会自动成为当前代理出口。
- 调整节点池模式，仅用户主动选择的普通节点会作为出口候选。
- 调整直连模式，仅保留内置“直连”出口启用。
- 调整本地代理模式，仅保留内置“本地代理”出口启用。
- 调整本地代理配置，允许修改默认本地代理端口，不再固定依赖 `7890`。
- 调整代理节点池列表布局，支持折叠和滚动限制，便于管理大量节点。
- 收紧代理节点行高度，让“直连 / 本地代理 / 最多 10 个节点”的区域保持紧凑，超出后滚动显示。
- 更新设置页与网络服务摘要，展示网关地址、出口模式、当前节点和已选节点数量。
- 更新代理池相关多语言文案，覆盖网关、出口、健康检测、选择模式和布局控制。
- 更新代理网络服务实现方案文档，记录已落地的网关、桥接、选择模型和健康检测逻辑。

### 修复
- 修复出口状态同步逻辑，选择直连、本地代理或节点池时会停用另外两类出口。
- 修复节点池选择逻辑，选择普通节点后会停用直连和本地代理出口。
- 修复直连与本地代理选择逻辑，切换到这两种模式时会清空普通节点的活动出口选择。
- 修复保存网络服务设置或更新代理池服务状态后，内置网关状态没有同步刷新的问题。
- 修复网关出口不可用或协议暂不可用时可能残留旧状态的问题，现在会回退到直连。
- 修复大量节点导致“网络服务”页面高度被过度撑开的布局问题。

## [0.0.13] - 2026-05-22

### 修复
- 修复 macOS universal 发布构建失败问题，确保代理订阅来源查询结果在 SQLite statement 释放前完成收集。

## [0.0.12] - 2026-05-22

### 新增
- 新增内置代理节点池 URL 订阅导入，支持 `http`/`https` 拉取校验、2 MB 内容限制、预览和选择性导入。
- 新增稳定哈希订阅 `source_id`，让同一订阅的重复导入和刷新都能绑定到同一个订阅来源。
- 在“网络服务”中新增订阅来源列表，可查看来源 URL、分组、节点数量、上次刷新时间和上次错误。
- 新增刷新单个订阅来源和刷新全部订阅来源操作。

### 变更
- 扩展代理池前端类型、服务层和 Tauri 命令，让节点列表同时返回订阅来源和刷新结果。
- 优化代理协议过滤，导入订阅中的协议也会出现在过滤列表中。

### 修复
- 订阅刷新拉取或解析失败时保留已有订阅节点，只记录 `last_error`，不再清空旧节点。
- 订阅节点只会在拉取和解析成功后按对应 `source_id` 原子替换。

## [0.0.11] - 2026-05-22

### 新增
- 新增第一版内置代理节点池数据库，支持 SQLite 持久化、迁移表、内置直连/本地代理节点、基础 CRUD 命令、启用/停用、搜索过滤元数据，以及节点凭据脱敏输出。
- 在“网络服务”中新增代理节点池面板，支持手动添加 `http`、`https`、`socks5` 节点，按名称/地址/分组搜索，按协议/分组过滤，删除单个或所选节点。
- 新增“添加资源”解析预览与选择性导入，支持 Clash YAML `proxies`、Base64 订阅文本，以及 `http`、`https`、`socks5`、`vmess`、`vless`、`trojan`、`ss`、`hysteria`、`hysteria2`、`tuic`、`anytls` 分享链接。
- 新增代理资源导入的结构化 YAML 解析能力。

### 变更
- 优化“内置代理内核”设置布局，让内核状态、缓存路径和操作按钮使用全宽响应式面板。
- 代理资源导入时凭据只在后端/数据库流程中处理，前端预览仅接收脱敏后的节点信息。

### 修复
- 修复代理池数据库初始化对 `rusqlite 0.32` 的 `pragma_update` 参数类型使用错误，恢复 Linux、macOS、Windows 发布构建。
- 保留 `0.0.10` 已补齐的 workspace `Cargo.lock` 更新，使 CI 发布构建可在锁定依赖模式下解析新的 YAML 解析依赖。

## [0.0.10] - 2026-05-22

### 新增
- 新增第一版内置代理节点池数据库，支持 SQLite 持久化、迁移表、内置直连/本地代理节点、基础 CRUD 命令、启用/停用、搜索过滤元数据，以及节点凭据脱敏输出。
- 在“网络服务”中新增代理节点池面板，支持手动添加 `http`、`https`、`socks5` 节点，按名称/地址/分组搜索，按协议/分组过滤，删除单个或所选节点。
- 新增“添加资源”解析预览与选择性导入，支持 Clash YAML `proxies`、Base64 订阅文本，以及 `http`、`https`、`socks5`、`vmess`、`vless`、`trojan`、`ss`、`hysteria`、`hysteria2`、`tuic`、`anytls` 分享链接。
- 新增代理资源导入的结构化 YAML 解析能力。

### 变更
- 优化“内置代理内核”设置布局，让内核状态、缓存路径和操作按钮使用全宽响应式面板。
- 代理资源导入时凭据只在后端/数据库流程中处理，前端预览仅接收脱敏后的节点信息。

### 修复
- 更新 workspace `Cargo.lock`，补齐代理资源 YAML 解析依赖，避免 CI 发布构建在锁定依赖模式下失败。

## [0.0.9] - 2026-05-22

### 新增
- 新增第一版内置代理节点池数据库，支持 SQLite 持久化、迁移表、内置直连/本地代理节点、基础 CRUD 命令、启用/停用、搜索过滤元数据，以及节点凭据脱敏输出。
- 在“网络服务”中新增代理节点池面板，支持手动添加 `http`、`https`、`socks5` 节点，按名称/地址/分组搜索，按协议/分组过滤，删除单个或所选节点。
- 新增“添加资源”解析预览与选择性导入，支持 Clash YAML `proxies`、Base64 订阅文本，以及 `http`、`https`、`socks5`、`vmess`、`vless`、`trojan`、`ss`、`hysteria`、`hysteria2`、`tuic`、`anytls` 分享链接。
- 新增代理资源导入的结构化 YAML 解析能力。

### 变更
- 优化“内置代理内核”设置布局，让内核状态、缓存路径和操作按钮使用全宽响应式面板。
- 代理资源导入时凭据只在后端/数据库流程中处理，前端预览仅接收脱敏后的节点信息。

## [0.0.8] - 2026-05-22

### 修复
- 修复自动更新签名校验失败问题，使应用内置 updater 公钥与发布产物签名密钥保持一致。
- 修复不可重试的签名错误仍显示为网络重试失败的问题。
- 修复数字版本 tag（例如 `0.0.8`）下“前往下载”的发布页 fallback 地址。

### 变更
- 新增发布时签名校验：当 `latest.json` 中的产物签名密钥与应用配置公钥不一致时，发布流程会直接失败。

## [0.0.7] - 2026-05-22

### 新增
- 新增内置代理内核状态命令，支持 xray 和 sing-box 的缓存准备、版本检测，以及从桌面应用打开内核缓存目录。
- 在“网络服务”设置中新增内置代理内核状态面板，可显示当前平台、资源目录、缓存目录、内核可用性、检测到的版本和清晰错误信息。

### 变更
- 扩展代理内核校验流程，在后续接入代理节点和桥接能力前，应用可先通过 `version` 命令确认缓存内核可执行。

## [0.0.6] - 2026-05-22

### 新增
- 新增内置代理内核第一阶段基础能力，为 xray 和 sing-box 固定 Windows、macOS Intel、macOS Apple Silicon、Linux x64、Linux arm64 多平台清单。
- 新增代理内核准备与校验脚本，可按当前构建目标下载、解压、校验 sha256，并只打包所需平台内核。
- 新增 Rust 侧运行时解析与缓存层，支持清单校验、sha256 校验、环境变量覆盖路径，以及复制到数据目录运行缓存。

### 变更
- 更新构建与发布工作流，让发布包可按平台带上正确的代理内核，包含 macOS universal 双架构内核。
- 发布工作流兼容 `0.0.6` 与 `v0.0.6` 两种 tag 格式。

## [0.0.5] - 2026-05-22

### 新增 
- **新增codex 自动加入功能

---
---
## [0.0.4] - 2026-05-22

### 新增 
- **新增 /v1/responses WebSocket 握手、鉴权、response.create 转发、SSE 到 WS 事件转发
- **优化布局逻辑结构 仪表盘服务控制台显示 Codex WS 地址和状态
- ** 增加多账号功能
- **增加服务面板功能 服务面板里增加 WebSocket 入口、状态和复制按钮 
- **优化仪表盘服务控制台里的按钮状态

---
## [0.0.3] - 2026-05-22

### 新增 
- **优化codex API 服务快速启动
- **优化布局逻辑结构
- ** 增加多账号功能
- **增加服务面板功能 刷新配额 订阅 模型供应商功能 唤醒任务 
- **增加会话管理 多开实例 

---
## [0.0.2] - 2026-05-21

### 新增 
- **仪表盘新增统一 API 服务控制台
- **新增主题：奶油色、星空色、海洋色
- **新增 ChatGPT Session Converter 模式


---
## [0.0.1] - 2026-05-21

### 新增
- **Codex 本地 API 服务现可选择上游代理模式**：API 服务设置可在跟随应用全局代理与直连官方上游之间切换，并将所选模式持久化用于网关请求。
- **Codex OAuth 授权现内置 2FA 快速取码入口**：添加账号弹框可展示已保存 MFA 密钥、刷新倒计时与一键复制验证码；重新授权时会显示并可复制目标账号邮箱。
- **Codex 本地 API 服务现支持自定义账号调度**：API 服务集合可选择“自定义”策略，为每个账号设置优先级与权重，批量调整已选账号，并把规范化后的调度规则写入网关选号逻辑。
- **Codex Token 导入现支持 ChatGPT/Codex session JSON**：可导入直接粘贴或包裹在 `session`/`session_json` 字段中的 session JSON，并复用现有 Codex OAuth 凭据导入流程。

### 变更
- **Codex 本地 API 服务上游连接失败现提供更可操作的网络/代理诊断**：网关会记录 502 失败状态，并把网络、代理或 `chatgpt.com` 可访问性问题提示成更清晰的错误信息。
### 新增
- **更新检查**: 实现通过 GitHub Releases API 自动检查更新。
  - 发现新版本时，右上角弹出精美的毛玻璃风格通知卡片。
  - 在 **设置 → 关于** 页面新增手动"检查更新"按钮，支持实时状态反馈。
  - 点击通知或下载按钮可直接跳转到 GitHub Release 页面下载。

---

### 技术栈
- 基于 Tauri 2.0 + React + TypeScript 构建。
- 使用 SQLite 数据库进行本地数据持久化。
- 使用系统钥匙串安全存储凭证。
- 跨平台支持（当前以 macOS 为主，Windows/Linux 计划中）。
